
// set a simple cookie
// set multiple simple cookies

// set a cookie with options
// set multiple cookies with options

// set the same cookie multiple times

import * as assert from 'assert'
import { createResponse } from '../support'

describe.skip('response.setCookie(name, value, options)', () => {
  // 
})
